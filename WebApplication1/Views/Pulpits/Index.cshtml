@model IEnumerable<WebApplication1.Models.Pulpit>

@{
    ViewData["Title"] = "Кафедры";
}

<h2>Список кафедр</h2>
<p>
    <a asp-action="Create">Добавить новую кафедру</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PulpitName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PulpitName)
                </td>
                <td>
                    <a asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
                </td>
            </tr>
        }
    </tbody>
</table>
